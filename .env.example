# Django REST API base URL, including the version path and no trailing slash.
VITE_FINANCE_API_URL=http://localhost:8000/api/v1

# Optional explicit Django login page used when the user session has expired.
# VITE_FINANCE_LOGIN_URL=http://localhost:8000/accounts/login/
