# قائمة تنفيذ التكامل الخلفي

- [x] فحص بنية مستودع Django `cms-backend` وبيئة قاعدة البيانات الحالية.
- [x] تثبيت نموذج مساحة العمل والمحافظ والحسابات والحركات في Django وترحيل قاعدة البيانات.
- [x] بناء نقاط Django REST للتحكم في المحافظ والحسابات والحركات والتقارير مع عزل مساحة العمل.
- [x] تجهيز إعدادات CORS والبيئة لربط تطبيق React بالخادم.
- [x] استبدال بيانات العرض في الواجهة بطبقة API وحالات تحميل وخطأ حقيقية.
- [x] اختبار إنشاء حركة، إعادة احتساب الرصيد، وعزل البيانات بين مساحات العمل.
- [x] تشغيل اختبارات البناء والخلفية ورفع التغييرات إلى فرع `dev` في المستودعات المعنية.

## تشغيل محلي عبر Docker Compose

- [x] مراجعة إعداد Docker الحالي في الخلفية ومتطلبات Vite في الواجهة.
- [x] إعداد Compose موحّد وملفات بيئة محلية للواجهة وDjango وPostgreSQL.
- [x] توثيق أمر بدء واحد، الترحيل، وتسجيل الدخول المحلي.

## تصحيح بنية المستودع

- [ ] نقل الخلفية المالية Django REST والترحيلات والاختبارات إلى repo `p2`.
- [ ] نقل Compose وملفات البيئة لتشغيل الواجهة والخلفية من repo واحد.
- [x] تعديل التوثيق وبناء المشروع واختبار الخلفية من repo `p2`.
- [x] إزالة النسخة الخلفية المكررة وملفات Compose من repo `cms-backend`.

## حزمة التحقق الشامل

- [x] فحص بنية الواجهة وبناء الإنتاج وملفات Compose.
- [x] تشغيل اختبارات Django والترحيلات وفحص النموذج.
- [x] اختبار API حي مع الجلسة وCSRF وCORS وعزل مساحة العمل.
- [x] توثيق نتائج الاختبارات والقيود المتبقية.
